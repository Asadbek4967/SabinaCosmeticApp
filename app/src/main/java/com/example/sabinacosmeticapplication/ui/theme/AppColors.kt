package com.example.sabinacosmeticapplication.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {

    // ===== Brand =====
    val Primary = Color(0xFF111827)
    val OnPrimary = Color(0xFFFFFFFF)

    val Accent = Color(0xFF2563EB)
    val OnAccent = Color(0xFFFFFFFF)

    // ===== Base surfaces =====
    val Background = Color(0xFFF6F7FB)
    val Surface = Color(0xFFFFFFFF)
    val SurfaceVariant = Color(0xFFF2F4F8)
    val SurfaceMuted = Color(0xFFEDF1F5)
    val SurfaceStrong = Color(0xFFE5EAF1)

    // ===== Text hierarchy =====
    val PrimaryText = Color(0xFF111827)
    val SecondaryText = Color(0xFF6B7280)
    val TertiaryText = Color(0xFF9CA3AF)
    val DisabledText = Color(0xFFD1D5DB)
    val InverseText = Color(0xFFFFFFFF)

    // ===== Borders / dividers =====
    val Border = Color(0xFFE5E7EB)
    val Divider = Color(0xFFEEF2F6)
    val Outline = Color(0xFFD7DCE5)

    // ===== Semantic =====
    val Success = Color(0xFF16A34A)
    val Warning = Color(0xFFD97706)
    val Error = Color(0xFFDC2626)
    val Info = Color(0xFF2563EB)

    // ===== Semantic backgrounds =====
    val SuccessBackground = Color(0xFFECFDF3)
    val WarningBackground = Color(0xFFFFF7ED)
    val ErrorBackground = Color(0xFFFEF2F2)
    val InfoBackground = Color(0xFFEFF6FF)

    // ===== Commerce =====
    val Price = Color(0xFFE53935)
    val OldPrice = Color(0xFF9CA3AF)

    // ===== Badges =====
    val DiscountBadgeBackground = Color(0xFFFFEBEE)
    val DiscountBadgeText = Color(0xFFC62828)

    val BestSellerBadgeBackground = Color(0xFFECFDF3)
    val BestSellerBadgeText = Color(0xFF15803D)

    val FlashSaleBadgeBackground = Color(0xFFFFF1F2)
    val FlashSaleBadgeText = Color(0xFFBE123C)

    // ===== Components =====
    val IconTint = Color(0xFF374151)
    val IconMuted = Color(0xFF6B7280)
    val ImagePlaceholder = Color(0xFFEDF1F5)
    val QuantityBackground = Color(0xFFF3F4F6)
    val InputBackground = Color(0xFFFFFFFF)

    // ===== State / interaction =====
    val PressedOverlay = Color(0x14000000)
    val DisabledBackground = Color(0xFFF3F4F6)
    val ShimmerBase = Color(0xFFF1F5F9)
    val ShimmerHighlight = Color(0xFFF8FAFC)
}